# firewall/allow-https.yaml
allow-https:
  name: allow-https-indev
  priority: 1000
  allow:
    - protocol: tcp
      ports:
        - "443"
  target_tags:
    - https-server
  source_ranges:
    - "0.0.0.0/0"
